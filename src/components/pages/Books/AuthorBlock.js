import React from "react"

const AuthorBlock = ({ children }) => {
  return (
    <div className="color-text-primary leading-none text-sm">
      {children}
    </div>
  )
}

export default AuthorBlock
